package me.kxda.hermes.commands.staff;

import me.kxda.hermes.utils.ColorText;
import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;
import org.bukkit.entity.Player;

import java.util.List;

/*public class VanishCommand implements CommandExecutor

   private final Vanish Vanish;
    private Object vanish;

    public Vanish() {
        this.vanish = Vanish.instance;
    }

    public boolean onCommand(final CommandSender sender, final Command command, final String label, final String[] args) {
        if (sender instanceof Player) {
            final Player p = (Player)sender;
            if (p.hasPermission("hermes.staff")) {
                if (args.length > 0) {
                    sender.sendMessage(ColorText.translate("&cYou don't have permission to use this command"));
                }
                else if (this.vanish.().isVanished(p)) {
                    this.vanish.VanishListener().setVanish(p, false);
                    sender.sendMessage(ColorText.translate("&7vanish has been &aenabled"));
*/